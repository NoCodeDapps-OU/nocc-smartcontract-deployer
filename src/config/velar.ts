import { VelarSDK } from '@velarprotocol/velar-sdk';

// Initialize Velar SDK with minimal config
export const velarSDK = new VelarSDK();

export const VELAR_CONFIG = {
  TOKENS: {
    NOCC: 'NOCC',
    STX: 'STX'
  }
};